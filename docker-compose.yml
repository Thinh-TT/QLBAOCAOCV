services:
  sql-edge:
    image: mcr.microsoft.com/azure-sql-edge
    container_name: qlbaocao-sql
    restart: always
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Thinh@123"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qlbaocao-web
    depends_on:
      - sql-edge
    environment:
      ASPNETCORE_ENVIRONMENT: Development

      ConnectionStrings__DefaultConnection: "Server=localhost,1434;Database=QLBaoCaoCV;User Id=sa;Password=Thinh@12345;TrustServerCertificate=True"
    ports:
      - "8080:8080"

volumes:
  sql_data:
